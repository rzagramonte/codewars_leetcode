/*
You task to pass only this tests :

a == false
!a == false
a == !a

P.R.E.P.
parameters: a variable
return: must return false on its own and with the logical operator ! but these two also must equal each other
examples: [] == false; ![] == false; [] == ![]
pseudocode:
declare const variable a and assign it to an empty array
*/

const a = [];